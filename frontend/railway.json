{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve dist/trendzy/browser -l $PORT --single",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/"
  }
}
